extends VBoxContainer

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	# add_theme_constant_override("separation", get_theme().get_meta("sweeper_game_ui_separation"))

